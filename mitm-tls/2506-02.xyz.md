#

```shell
sudo ip netns exec net2 openssl s_server -port 9999  -cert ./certs/server.pem -key ./certs/server_priv.pem -tls1_3

```


```shell

sudo ip netns exec net1 openssl s_client -connect 192.168.64.6:9999 -CAfile ./certs/ca.pem
```

#

```shell
Post-Handshake New Session Ticket arrived:
SSL-Session:
    Protocol  : TLSv1.3
    Cipher    : TLS_AES_256_GCM_SHA384
```